/* _config.yml v1.0.0_202510221638 */

# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you: 
# https://learn-the-web.algonquinde.com/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: "GOFUNWITH"
description: "Go have fun with tech, tools, and life."
author: "GOFUNWITH Team"
email: "master@gofunwith.com"

# baseurl: "" # the subpath of your site, e.g. /blog
# CNAME 파일 사용으로 baseurl은 "" (empty)로 설정합니다.
baseurl: ""
# theme.yml의 url 값을 site.url로 사용합니다.
url: "https://blog.gofunwith.com" 

# GitHub Pages 호환 플러그인
plugins:
  - jekyll-seo-tag
  - jekyll-feed
  - jekyll-sitemap

# Build settings
# theme: minima # 기본 테마 대신 커스텀 테마 사용
sass:
  sass_dir: _sass
  style: compressed

# Jekyll 기본 설정
markdown: kramdown
kramdown:
  input: GFM
  syntax_highlighter: rouge

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal default list.
#
# Excluded items can be processed by explicitly listing the directories or
# files' path in the `include:` list.
#
exclude:
  - .sass-cache/
  - .jekyll-cache/
  - gemfiles/
  - Gemfile
  - Gemfile.lock
  - node_modules/
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
  - Dockerfile
  - docker-compose.yml

# Include
# /admin/ 경로를 Jekyll이 빌드하도록 명시적으로 포함
include:
  - admin

# Liquid
liquid:
  error_mode: strict
  strict_filters: true
  strict_variables: true

# Defaults
# 각 타입별 기본 front matter 설정
defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
      author: "GOFUNWITH"
      category: "default"
  - scope:
      path: ""
      type: "pages"
    values:
      layout: "default"
  - scope:
      path: "admin" # Admin 폴더 내 파일은 layout 비활성화
    values:
      layout: none
